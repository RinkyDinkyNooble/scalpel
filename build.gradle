// Root project only holds shared settings. Code lives in core/ and the port folders.

subprojects {
    group = mod_group_id
    version = mod_version

    repositories {
        mavenCentral()
    }

    tasks.withType(JavaCompile).configureEach {
        options.encoding = 'UTF-8'
        options.release = 17
    }
}
